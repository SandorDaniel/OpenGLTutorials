// For original version see common/shader.hpp

#ifndef SHADER_HPP
#define SHADER_HPP

GLuint LoadShaders(std::vector<const char*> v_vertex_file_path, std::vector<const char*> v_fragment_file_path);

#endif
